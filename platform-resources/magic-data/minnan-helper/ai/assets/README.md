# Minnan Helper AI Assets

当前目录是 `magic-data/minnan-helper` 接入统一 AI framework 的资产占位目录。

当前阶段：

- 闽南语助手仍沿用 `backend/ai-service.js` 内的 prompt/template 和 `backend/lexicon/minnan-lexicon.json`；`backend/lexicon/minnan-lexicon.csv` 只保留为参考源。
- 本轮先完成 adapter 桥接，不一次性迁移旧业务层。

后续迁移目标：

- prompt
- rules
- schema
- defaults
- lexicon 入口说明
