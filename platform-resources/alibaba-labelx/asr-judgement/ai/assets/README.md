# ASR Judgement AI Assets

当前目录是 `alibaba-labelx/asr-judgement` 接入统一 AI framework 的资产占位目录。

当前阶段：

- 快判 AI 仍沿用 `backend/ai/` 下的规则与 Prompt 资产：
  - `rules-v2.ai.md`
  - `listen-prompt-template.md`
  - `compare-prompt-template.md`
  - `fewshot-examples.json`
- 本轮先完成 `suggest` 主链路的 framework 桥接，不一次性迁移 Prompt、规则和 defaults 资产文件。

后续迁移目标：

- prompt
- rules
- defaults
- schema
- few-shot
