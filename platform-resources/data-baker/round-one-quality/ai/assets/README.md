# DataBaker AI Assets

当前目录是 `platform-resources/data-baker/round-one-quality/ai/` 的资产占位目录。

当前阶段：

- DataBaker 仍沿用 `backend/ai-service.js` 内的 prompt/template 和 `backend/reference/minnan-lexicon.json`；`backend/reference/minnan-lexicon.csv` 只保留为参考源。
- 本轮先把 adapter 接入统一 framework，暂不一次性搬空旧业务层。

后续迁移会逐步把以下内容迁到这里：

- prompt
- rules
- schema
- defaults
- lexicon 入口说明
