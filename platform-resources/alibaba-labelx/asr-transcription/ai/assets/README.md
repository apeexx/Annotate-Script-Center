# ASR Transcription AI Assets

当前目录是 `alibaba-labelx/asr-transcription` 接入统一 AI framework 的资产占位目录。

当前阶段：

- 转写 AI 仍沿用 `backend/ai-prompts.js` 与 `backend/ai/ai-rules.md` 的 Prompt / 规则体系。
- 本轮先完成 `suggest-current` 的 framework 桥接，不一次性迁移 Prompt、规则和 defaults 资产文件。

后续迁移目标：

- prompt
- rules
- defaults
- schema
