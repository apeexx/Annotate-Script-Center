# 05-我的团队 DOM 结构

## 页面标识 / 路由 / 前置条件

- 路由：`/organization/myteam`
- 框架：Vue 2 + Element UI

## 页面总览

- 当前页主要记录稳定区域、可见文案和角色边界。

## DOM 树 / 区域结构

- 当前文件未补充完整 DOM 树；后续仅记录稳定区域结构。

## 稳定选择器表

- 优先使用 route、标题文案、稳定输入框和原生按钮文本，不依赖 hash class。

## 动态区域 / 重渲染风险

非核心标注链路，未做详细 DOM 采集。页面包含机构选择、团队列表、用户列表等组织管理功能。

## 可挂载点建议

- 如需挂载扩展 UI，优先选择宿主页面外层安全区域，不覆盖原生写操作控件。

## 页面区域与接口映射

- 当前文件未补充更细的接口映射；新增时只记录稳定区域与请求族对应关系。

## 写操作边界 / 未确认项

非核心标注链路，未做详细 DOM 采集。页面包含机构选择、团队列表、用户列表等组织管理功能。
