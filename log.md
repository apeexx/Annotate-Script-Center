# 修改日志

## 2026-07-22：发布 0.4.3 粤语助手手绘分段兼容修复

- 在真实登录 Aishell 标注页完成验收后，将当前分段兼容修复整理为 `0.4.3`：无纯数字主段时，严格说话人节点与未保存的 Wavesurfer 手绘节点可按波形位置建立统一目录。
- 未保存手绘节点仅接受空标签、唯一 `wavesurfer_` 前缀 ID 和有效 CSS 几何；与 `1…N` 区段按钮精确对应后按 CSS `left` 映射，同起点再按区域 DOM 顺序稳定排序。未知标签、异常或重复临时 ID、无效几何和按钮不一致仍安全拒绝，绝不发送整段音频。
- 本次重新生成 public CRX、ZIP、自动更新 XML 和最新版本描述；服务端仅需同步下载产物，无需重启后端、PM2 或 Nginx。

## 2026-07-22：兼容希尔贝壳粤语助手未保存手绘分段

- 已确认未保存的手绘区段保留为空 `data-region-label`，但有 `wavesurfer_` 前缀临时 ID 与有效波形 CSS 几何；此前被误判为未知覆盖层，导致同一音频的全说话人目录整体拒绝。
- 无纯数字主段时，仅允许这种临时节点与严格 S1–S4 说话人节点共同按 CSS `left` 映射；同起点仍以区域 DOM 顺序排序，并继续要求所有节点唯一、几何有效且按钮精确为 `1…N`。非空未知标签、临时 ID 异常或重复仍拒绝，不回退整段音频。
- 新增单个/多个未保存手绘段、同起点、临时节点几何与 ID 异常、混合目录按钮校验的回归覆盖；本次未提升版本、未打包、未上传下载站。

## 2026-07-22：修复希尔贝壳粤语助手同起点说话人分段

- 已确认部分全说话人音频的不同说话人节点会拥有相同 CSS `left`，但 ID、标签和宽度不同，属于有效的同时说话，不再使整条音频的全部分段失败。
- 说话人目录继续以 CSS `left` 为主排序；仅同一 `left` 时以波形区域 DOM 顺序作次级排序。该规则同时适用于无纯数字主段的全说话人目录与纯数字主段前的说话人前缀，不读取局部编号或标题时间。
- 保留纯数字主段优先、按钮精确映射、CSS 几何、当前选择、截取时长与裁剪范围校验；本次未提升扩展版本、未打包、未上传下载站。

## 2026-07-21：准备发布 0.4.2 粤语助手前缀说话人分段修复

- 用户已确认完成真实登录 Aishell 标注页验收；前缀说话人节点按波形 CSS 位置映射和纯数字主段优先规则进入 `0.4.2`。
- 已重新生成 public CRX、ZIP、更新 XML 与 latest JSON，并核对版本、文件名、文件大小及 SHA-256 一致；下载站尚待按正式产物同步，未改动后端服务。

## 2026-07-20：修复希尔贝壳粤语助手前缀说话人标签分段

- 已确认“说话人 Sx:编号”中的编号是该说话人的局部序号。唯一纯数字主段始终优先；若最小纯数字主段为 `N>1`，仅在左侧恰好有 `N-1` 个有效严格说话人节点且 CSS `left` 不并列时，才按位置升序映射为全局 `1…N-1`，不读取局部编号、DOM 顺序或标题。
- 重复主段、无效几何和无效前缀均改为该编号的预检失败：单条只拒绝当前段，安全纯数字段不会被连带阻断。批量从开始计入失败段且不裁剪、不发 AI；仅当前段预检失败或缺时长、比例/范围无效时切换到最小安全纯数字段校准，按钮映射与选择同步错误不自动切换。
- 新增真实前缀 `S3:1 / S4:1 / S3:2 / S2:1 → 1…4`、安全主段隔离、批量预检跳过和校准切换回归覆盖，并更新平台说明；候选 `0.4.2` 产物须在真实页面复测通过后重新打包。

## 2026-07-20：统一修复希尔贝壳粤语助手的手绘区段与说话人覆盖层解析

- 手绘蓝色区段的裁剪改以当前“截取时长”校准区段 CSS `left / width` 的像素时间；区域标题仅保留为非阻断诊断，不再参与裁剪范围或识别拒绝。
- S1/S2 等非纯数字说话人覆盖层继续完全排除在可识别区段目录、裁剪、结果键和批量队列之外；稀疏编号仍可用，编号重复、按钮映射、当前选择、时长、CSS 几何和音频实际范围仍保留安全拦截。
- 已生成本地 `0.4.2` 候选 CRX、ZIP、自动更新 XML 与 latest JSON，下载站尚未上传；真实登录标注页验收与两台设备安装同一 `0.4.2` 包的复测尚未在当前会话完成。

## 2026-07-20：修复希尔贝壳粤语助手的稀疏区段编号兼容

- 修复其他设备上合法蓝色区段编号不连续时，单条 AI 识别错误拒绝的问题：仅要求可识别区段编号唯一，不再要求从 `1` 开始连续；说话人 S1–S4 覆盖层仍只作为页面元数据，不参与裁剪、结果绑定或批量。
- 批量改为按真实存在的区段编号匹配页面按钮，不再按数组下标或按钮总数推断；同一音频的批量任务保持原条目索引，不会因稀疏区段编号误切换其他音频条目。
- 新增稀疏编号单条解析与批量按钮映射回归测试，并同步更新粤语助手与 Aishell 平台资料；已重新打包同版本 `0.4.1` 产物，未改动后端。

## 2026-07-20：重新打包 0.4.1 public CRX 产物

- 在不提升 `extension/manifest.json` 版本号的前提下，重新生成 public `0.4.1` 的 CRX、ZIP、更新 XML 和最新版本描述；产物包含希尔贝壳粤语助手说话人覆盖层兼容修复。
- 已校验 CRX 哈希与最新版本描述一致，ZIP 包含更新后的片段解析器；未上传下载站、未部署后端。相同版本号不能触发已安装扩展的企业自动更新，需手动分发或重新安装。

## 2026-07-20：修复希尔贝壳粤语助手的说话人覆盖层兼容

- 粤语助手改为仅以纯数字 `data-region-label` 建立可识别区段目录，并和页面当前选择精确匹配；说话人 S1–S4 覆盖层不再影响音频裁剪、结果绑定或批量任务。
- 编号重复、缺失或不连续仍会安全拒绝识别；单条与两种批量都不会读取、修改或自动切换页面说话人选择。
- 新增覆盖层前后插入、重复编号与缺失编号回归测试，并同步更新粤语助手与 Aishell 页面结构说明；未打包、未部署。

## 2026-07-20：修复希尔贝壳粤语助手在弹窗中未命中

- 补齐弹窗的希尔贝壳脚本候选列表。已启用的粤语助手在 `/mytask/mark` 页现在会正确显示为“粤语助手”，并可使用“打开当前脚本设置”入口；不影响其他语言助手的互斥与运行时行为。

## 2026-07-20：希尔贝壳粤语助手改为蓝色区段精确识别

- 将粤语识别从整条音频直传改为当前蓝色区段裁剪：浏览器按波形 DOM 顺序、当前选择和截取时长校准范围，离线生成 `16kHz` 单声道 WAV Base64；任何映射或裁剪失败均拒绝识别，不回退整段音频。
- 单条结果键绑定任务条目与区段 ID/起止时间；批量“全部”覆盖当前音频全部区段，“未完成”逐段检查文本框。AI 可并发预取但先按区段顺序缓冲，再串行切段、原样填入、点击真实保存并等待成功提示；空结果仅作人工复核。
- 后端强制片段字段与 WAV Data URL 契约，模型只接收裁剪音频；增加 `3MB` 结构化超限响应、片段音频摘要缓存隔离与音频/来源/凭据调试脱敏。
- 保存前再次确认当前条目和蓝色区段；停止会取消待发与进行中的裁剪/AI，已开始的原生保存安全收尾。
- 同步更新粤语运行时与后端说明、Aishell 页面结构、平台索引；未进行真实登录任务验收、未打包、未部署。

## 2026-07-20：重新接入希尔贝壳粤语助手

- 修复粤语原始听写将空平台参考文本误判为请求错误的问题：保留空 `referenceText` 作为条目上下文，但只要条目 ID 与音频地址有效就继续识别。
- 修复系统管理页的后端切换自锁：后端模式与根地址设置现在显示在管理员密码入口之前。服务器模式未配置管理员鉴权时，仍可先切换到本机后端完成本地验收。
- 新增独立脚本 `aishellTechCantoneseAssistant`，与希尔贝壳闽南语、越南语、泰语和中英短剧互斥启用；工作范围固定为 `/mytask/mark`。
- 粤语 AI 采用台州话式单次 Qwen Omni 原始听写：默认 `qwen3.5-omni-plus`，可切换 `qwen3.5-omni-flash`，强制关闭 thinking、超时 60 秒；结果只保留原样 `listenText`，不增加语速、词表、转换、比较或 Fun-ASR 阶段。
- 运行时复用 Aishell 的 `dataRoot + url` 音频拼接、任务轮询、缓存、批量预取和串行真实保存边界；单条只填入，批量可停止，保存始终点击页面真实按钮。
- 同步注册统一后端路由、扩展 manifest、存储迁移、Options 设置、平台索引与运行时/后端说明；未打包、未部署、未推送。

## 2026-07-19：移除希尔贝壳粤语助手以便重新实现

- 删除粤语助手的扩展运行时、平台资料、独立后端、测试、AI 调用日志数据集及本地运行日志。
- 同步移除扩展 manifest、脚本库、设置页、存储归一化、统一后端注册、AI 日志导出和平台索引中的粤语入口；保留被其他语言共用的异步任务取消能力。
- `dist/` 产物恢复到接入粤语前的版本内容；本次未重新打包、未部署、未推送。

## 2026-07-13：恢复 public 工作台与 12 个现行脚本设置入口

- 恢复 options 工作台的功能面板、脚本详情、下载中心和系统管理视图；功能卡片仍只由当前 `SCRIPT_LIBRARY` 和 public 可见性规则生成，范围固定为现行 12 个脚本。
- 恢复“打开设置”、脚本启停、基础设置、适用的 AI 参数、快捷键录制/清空和保存流程，继续兼容已有 `chrome.storage` 数据，不清空用户已保存配置。
- 恢复管理员会话、仪表盘、下载中心、项目数据导出和 AI 调用记录导出的前端入口；后端只校验并使用当前仍注册的管理与 AI 路由。
- 工作台不重新暴露已删除历史脚本、其运行时代码、路由、词表或资料；public 发布通道、默认后端地址和扩展版本保持不变。

## 2026-07-13：更新 Magic Data 客家话七月正字表

- 替换客家话正字表原始 Excel，运行时词表同步更新为 690 条，来源仅包含“正字”和“补充正字”工作表；“疑问”工作表保留在原始文件中但不导入。
- CSV 参考源与 JSON 运行时主词表同步重建；新版转换脚本支持拆分 `正字/别名` 写法，并将备注保留在 JSON 词条中。

## 2026-07-13：v0.4.1 public 发布准备

- 将新安装扩展、CRX 自动更新地址、下载中心与后端默认地址迁移至 `script.aisiyunling.com`，保留旧域名权限与已保存旧设置的兼容入口。
- 仅保留 public 发布通道，并将正式发布版本更新为 `0.4.1`。
- CRX 打包在既有 `annotation-script-center.pem` 缺失时会直接中止，不再生成新私钥，避免扩展 ID 变化。
- 正式 CRX 打包、上传与自动更新发布仍待找回原签名私钥后执行。
- 修复 Windows 临时目录与发布目录跨盘时的 CRX 移动兼容：仅在 `EXDEV` 下复制成功后清理本次临时 CRX，其他移动错误保持失败；新增回归测试。
- 已复用既有签名私钥生成 v0.4.1 public CRX、public ZIP 与自动更新三件套；扩展 ID 保持 `megaonhojlkfhkdbhfapeknjokhgmkad`，未生成 beta 产物。
