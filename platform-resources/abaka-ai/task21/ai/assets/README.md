# Task21 AI Assets

当前目录是 `abaka-ai/task21` 接入统一 AI framework 的资产占位目录。

当前阶段：

- Task21 仍沿用 `backend/prompt.js` 和 `backend/ai/prompt.md` 的 Prompt 体系。
- 本轮先完成 adapter 桥接，不一次性迁移两阶段视觉链路的旧业务层。

后续迁移目标：

- prompt
- rules
- schema
- defaults
- visual / reasoning 资产入口说明
